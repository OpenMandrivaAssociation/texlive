diff -up texlive-20260301-source/texk/web2c/pdftexdir/pdftoepdf.cc.omv~ texlive-20260301-source/texk/web2c/pdftexdir/pdftoepdf.cc
--- texlive-20260301-source/texk/web2c/pdftexdir/pdftoepdf.cc.omv~	2026-05-09 00:56:08.557994675 +0200
+++ texlive-20260301-source/texk/web2c/pdftexdir/pdftoepdf.cc	2026-05-09 00:59:56.688154953 +0200
@@ -427,7 +427,7 @@ static void copyFont(char *tag, Object *
         charset = fontdesc.dictLookup("CharSet");
         if (!charset.isNull() &&
             charset.isString() && is_subsetable(fontmap))
-            epdf_mark_glyphs(fd, (char *)charset.getString()->c_str());
+            epdf_mark_glyphs(fd, (char *)charset.getString().c_str());
         else
             embed_whole_font(fd);
         addFontDesc(fontdescRef.getRef(), fd);
@@ -555,7 +555,7 @@ static void copyObject(Object * obj)
     int i, l, c;
     Ref ref;
     char *p;
-    GString *s;
+    std::string s;
     if (obj->isBool()) {
         pdf_printf("%s", obj->getBool()? "true" : "false");
     } else if (obj->isInt()) {
@@ -565,9 +565,9 @@ static void copyObject(Object * obj)
     } else if (obj->isNum()) {
         pdf_printf("%s", convertNumToPDF(obj->getNum()));
     } else if (obj->isString()) {
-        s = (GooString *)obj->getString();
-        p = (char *)s->c_str();
-        l = s->size();
+        s = obj->getString();
+        p = (char *)s.c_str();
+        l = s.size();
         if (strlen(p) == (unsigned int) l) {
             pdf_puts("(");
             for (; *p != 0; p++) {
@@ -583,7 +583,7 @@ static void copyObject(Object * obj)
         } else {
             pdf_puts("<");
             for (i = 0; i < l; i++) {
-                c = s->getChar(i) & 0xFF;
+                c = static_cast<unsigned char>(s[i]);
                 pdf_printf("%.2x", c);
             }
             pdf_puts(">");
diff -up texlive-20260301-source/texk/web2c/pdftexdir/pdftosrc.cc.omv~ texlive-20260301-source/texk/web2c/pdftexdir/pdftosrc.cc
--- texlive-20260301-source/texk/web2c/pdftexdir/pdftosrc.cc.omv~	2026-05-09 01:00:29.904632764 +0200
+++ texlive-20260301-source/texk/web2c/pdftexdir/pdftosrc.cc	2026-05-09 01:15:15.505754910 +0200
@@ -67,7 +67,7 @@ int main(int argc, char *argv[])
     Stream *s;
     Object srcStream, srcName, catalogDict;
     FILE *outfile;
-    char *outname;
+    std::string outname;
     int objnum = 0, objgen = 0;
     bool extract_xref_table = false;
     int c;
@@ -107,9 +107,7 @@ int main(int argc, char *argv[])
             fprintf(stderr, "No SourceName found\n");
             exit(1);
         }
-        outname = (char *)srcName.getString()->c_str();
-        // We cannot free srcName, as objname shares its string.
-        // srcName.free();
+        outname = srcName.getString();
     } else if (objnum > 0) {
         srcStream = xref->fetch(objnum, objgen);
         if (!srcStream.isStream()) {
@@ -132,8 +130,8 @@ int main(int argc, char *argv[])
         sprintf(p, ".xref");
         outname = buf;
     }
-    if (!(outfile = fopen(outname, "wb"))) {
-        fprintf(stderr, "Cannot open file \"%s\" for writing\n", outname);
+    if (!(outfile = fopen(outname.c_str(), "wb"))) {
+        fprintf(stderr, "Cannot open file \"%s\" for writing\n", outname.c_str());
         exit(1);
     }
     if (extract_xref_table) {
@@ -192,11 +190,11 @@ int main(int argc, char *argv[])
             fputc(c, outfile);
     }
     if (objnum == 0)
-        fprintf(stderr, "Source file extracted to %s\n", outname);
+        fprintf(stderr, "Source file extracted to %s\n", outname.c_str());
     else if (objnum > 0)
-        fprintf(stderr, "Stream object extracted to %s\n", outname);
+        fprintf(stderr, "Stream object extracted to %s\n", outname.c_str());
     else
-        fprintf(stderr, "Cross-reference table extracted to %s\n", outname);
+        fprintf(stderr, "Cross-reference table extracted to %s\n", outname.c_str());
     fclose(outfile);
     delete doc;
 }
